import '/src/assets/css/Icon.less';

// 下拉图标
export const AdminDownIcon = ({ className }) => {
  return <i className={`change-down-icon ${className}`}></i>;
};

// 收缩图标
export const AdminUpIcon = ({ className }) => {
  return <i className={`change-up-icon ${className}`}></i>;
};
